# 面试猿刷题平台

面试八股文智能刷题系统 - 专业的技术面试题库与学习平台

## 📖 项目简介

面试猿刷题平台是一个专业的面试八股文智能刷题系统，致力于为求职者提供系统化、结构化的面试题目练习体验。平台采用现代化的技术栈，提供流畅的用户体验和强大的管理功能。

### 核心特性

- 🎯 **系统化题库**：按技术分类组织题目，支持精细化管理
- 🔒 **答案隐藏机制**：模拟真实面试场景，先思考后查看答案
- 📝 **Markdown 支持**：富文本题目和答案编辑，支持代码高亮
- 👤 **完整用户系统**：登录注册、个人中心、学习进度跟踪
- 💬 **评论互动**：支持题目评论和回复，构建学习社区
- 🎨 **现代化 UI**：基于 shadcn/ui 的精美组件，响应式设计
- 🔐 **权限管理**：管理员后台，支持题库和题目的完整管理
- 📊 **进度跟踪**：个人答案记录、题目收藏、学习统计

## 🛠️ 技术栈

### 核心框架
- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript (严格模式)
- **样式**: Tailwind CSS + tailwindcss-animate
- **状态管理**: Zustand
- **数据获取**: React Query (@tanstack/react-query)
- **HTTP 客户端**: Axios

### UI 组件
- **组件库**: shadcn/ui (基于 Radix UI)
- **图标**: Lucide React
- **表单**: React Hook Form + Zod 验证
- **Markdown**: @uiw/react-md-editor + @uiw/react-markdown-preview
- **Toast 通知**: Radix UI Toast

### 开发工具
- **代码规范**: ESLint + Prettier
- **类型工具**: class-variance-authority, clsx, tailwind-merge
- **文件上传**: Ali OSS

## 📁 目录结构

```
mianshiyuan-frontend/
├── app/                      # 页面路由 (Next.js App Router)
│   ├── page.tsx             # 首页 - 题库列表
│   ├── banks/               # 题库相关页面
│   │   └── [bankId]/        # 题库详情页
│   ├── question/            # 题目相关页面
│   │   └── [questionId]/    # 题目详情页
│   ├── questions/           # 题目管理页面
│   │   ├── add/            # 新增题目
│   │   └── edit/           # 编辑题目
│   ├── profile/             # 个人中心页面
│   └── admin/               # 管理后台
│       ├── banks/          # 题库管理
│       └── questions/      # 题目管理
├── components/              # 可复用组件
│   ├── ui/                 # shadcn/ui 基础组件
│   ├── layout/             # 布局组件（Navbar, Hero, Container）
│   ├── auth/               # 认证组件（登录、注册、用户菜单）
│   ├── bank/               # 题库组件
│   ├── question/           # 题目组件（详情、评论、答案）
│   ├── profile/            # 个人中心组件
│   ├── admin/              # 管理后台组件
│   └── home/               # 首页组件
├── lib/                     # 工具函数和 API 客户端
│   ├── api/                # API 客户端模块
│   │   ├── client.ts       # Axios 客户端配置
│   │   ├── auth.ts         # 认证相关 API
│   │   ├── bank.ts         # 题库相关 API
│   │   ├── question.ts     # 题目相关 API
│   │   ├── user.ts         # 用户相关 API
│   │   └── file.ts         # 文件上传 API
│   ├── config/             # 配置文件
│   ├── oss/                # OSS 上传工具
│   └── utils/              # 工具函数
├── hooks/                   # 自定义 React Hooks
│   ├── use-auth.ts         # 认证相关 Hooks
│   ├── use-banks.ts        # 题库相关 Hooks
│   ├── use-toast.ts        # Toast 通知 Hook
│   └── use-debounce.ts     # 防抖 Hook
├── types/                   # TypeScript 类型定义
│   └── index.ts            # 全局类型定义
├── store/                   # Zustand 状态管理
│   └── user-store.ts       # 用户状态管理
└── doc/                     # 项目文档
    ├── 产品需求文档.md     # 产品 PRD
    └── 主页原型.html       # 页面原型
```

## ✨ 功能模块

### 用户端功能

#### 1. 首页模块 (`/`)
- 📚 展示所有题库列表（卡片式布局）
- 🏷️ 显示题库标题、描述、题目数量、标签
- 🔍 支持题库搜索和筛选
- 📱 响应式设计，移动端友好

#### 2. 题库详情 (`/banks/[bankId]`)
- 📋 展示题库下的所有题目列表
- 🎚️ 支持难度筛选（简单、中等、困难）
- 🏷️ 支持标签筛选
- 🔍 题目搜索功能
- 📄 分页加载

#### 3. 题目详情 (`/question/[questionId]`)
- 📝 完整的题目信息展示
- 🔒 **答案隐藏机制**：默认隐藏答案，需要主动查看
- 📖 Markdown 格式渲染题目和答案
- 💬 评论和回复功能
- 📊 浏览量、收藏数统计
- ⭐ 收藏功能
- 🎯 题目导航（上一题/下一题）

#### 4. 个人中心 (`/profile`)
- 👤 个人信息展示和编辑
- 📝 我的答案记录
- 📚 我的创建题目
- ⚙️ 个人设置（通知、隐私等）
- 🎨 头像上传

#### 5. 题目管理
- ➕ 新增题目 (`/questions/add`)
- ✏️ 编辑题目 (`/questions/edit/[questionId]`)
- 📝 Markdown 编辑器支持
- 🏷️ 标签管理
- 🎚️ 难度设置

### 管理端功能 (`/admin/*`)

#### 1. 题库管理 (`/admin/banks`)
- 📊 题库列表展示（表格形式）
- ➕ 新增题库
- ✏️ 编辑题库信息
- 🗑️ 删除题库
- 🖼️ 封面图片上传
- 🔍 搜索和分页
- 📋 状态管理（草稿、待审核、已发布）

#### 2. 题目管理 (`/admin/questions`)
- 📊 题目列表展示
- ✏️ 批量编辑
- 🗑️ 批量删除
- 🔗 题目与题库绑定
- 📥 批量导入功能
- 🔍 高级搜索筛选
- 📋 状态审核流程

#### 3. 权限控制
- 🔐 管理员权限验证
- 🚪 路由守卫
- 👥 角色管理（USER、ADMIN、REVIEWER）

## 🚀 快速开始

### 前置要求

- Node.js 18.x 或更高版本
- npm 或 yarn 包管理器
- 后端 API 服务运行在 `http://localhost:8080`

### 安装依赖

```bash
npm install
```

### 配置环境变量

复制并配置环境变量：
```bash
cp .env.example .env.local
```

编辑 `.env.local` 文件，配置 API 地址和其他必要参数。

### 开发环境

启动开发服务器：
```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 查看应用。

开发服务器特性：
- 🔥 热模块替换（HMR）
- ⚡ 快速刷新
- 📝 TypeScript 类型检查
- 🎨 Tailwind CSS JIT 编译

### 构建生产版本

```bash
# 构建
npm run build

# 启动生产服务器
npm start
```

### 代码质量

```bash
# 代码格式化
npm run format

# 代码检查
npm run lint

# 自动修复 lint 问题
npm run lint -- --fix
```

## 📚 开发指南

### 创建新页面

1. 在 `app/` 目录下创建路由文件夹
2. 添加 `page.tsx` 文件
3. 导出默认组件

```typescript
// app/my-page/page.tsx
export default function MyPage() {
  return <div>My Page</div>;
}
```

### 创建新组件

1. 在 `components/` 目录对应模块下创建组件
2. 使用 TypeScript 定义 Props
3. 导出组件

```typescript
// components/my-component.tsx
interface MyComponentProps {
  title: string;
}

export function MyComponent({ title }: MyComponentProps) {
  return <div>{title}</div>;
}
```

### 添加 shadcn/ui 组件

```bash
# 添加新组件
npx shadcn@latest add [component-name]

# 例如：添加 Button 组件
npx shadcn@latest add button
```

### API 调用

1. 在 `lib/api/` 目录下定义 API 函数
2. 在 `types/index.ts` 中定义类型
3. 使用 React Query 进行数据获取

```typescript
// lib/api/my-api.ts
import { apiClient } from "./client";
import type { MyData } from "@/types";

export async function getMyData(): Promise<MyData> {
  const response = await apiClient.get<MyData>("/my-data");
  return response.data;
}

// 在组件中使用
import { useQuery } from "@tanstack/react-query";
import { getMyData } from "@/lib/api/my-api";

export function MyComponent() {
  const { data, isLoading } = useQuery({
    queryKey: ["myData"],
    queryFn: getMyData,
  });
  
  if (isLoading) return <div>Loading...</div>;
  return <div>{data?.name}</div>;
}
```

## 🔍 常见问题

### API 请求失败

**问题**：API 请求返回 401 或 403 错误

**解决方案**：
1. 检查后端服务是否正常运行
2. 确认 Token 是否有效（在 localStorage 中查看）
3. 检查 API 地址配置是否正确

### 构建错误

**问题**：`npm run build` 失败

**解决方案**：
1. 删除 `.next` 文件夹
2. 清除缓存：`npm cache clean --force`
3. 重新安装依赖：`rm -rf node_modules && npm install`
4. 检查 TypeScript 类型错误

### 样式不生效

**问题**：Tailwind CSS 样式未应用

**解决方案**：
1. 确认 `globals.css` 已在 `layout.tsx` 中导入
2. 检查 `tailwind.config.ts` 配置
3. 重启开发服务器

## 🎨 设计规范

### 主题色

- **主色**: `#111827` (深灰色) - 传达专业感
- **强调色**: `#facc15` (黄色) - 突出重要信息
- **状态色**：
  - 简单：`#10b981` (翠绿色)
  - 中等：`#3b82f6` (蓝色)
  - 困难：`#8b5cf6` (紫色)

### 响应式断点

- **移动端**: `< 900px` - 单列布局，优化触控体验
- **平板**: `768px - 1199px` - 双列布局
- **桌面端**: `≥ 1200px` - 多列布局，最佳浏览体验

### UI 组件设计原则

- 🎯 **一致性**：使用 shadcn/ui 统一组件风格
- 🎨 **卡片化**：内容容器采用卡片设计
- 🎭 **动画效果**：适度使用过渡动画提升体验
- 📱 **触控友好**：按钮和交互区域足够大

## 💻 代码规范

### 组件开发
- ✅ 使用函数式组件 + React Hooks
- ✅ TypeScript 严格模式（`strict: true`）
- ✅ 组件 Props 必须有类型定义
- ✅ 使用 `"use client"` 标记客户端组件
- ✅ 优先使用服务端组件（默认）

### API 调用
- ✅ 所有 API 调用必须有类型定义
- ✅ 使用 React Query 管理服务端状态
- ✅ 统一的错误处理和 Toast 提示
- ✅ 请求拦截器自动添加 Token

### 文件命名
- ✅ 组件文件：`kebab-case.tsx` (例: `user-menu.tsx`)
- ✅ 类型文件：`index.ts`
- ✅ Hook 文件：`use-*.ts` (例: `use-auth.ts`)
- ✅ API 文件：`*.ts` (例: `auth.ts`)

### 代码组织
```typescript
// 1. 导入语句
import { useState } from "react";
import { Button } from "@/components/ui/button";

// 2. 类型定义
interface Props {
  title: string;
}

// 3. 组件定义
export function MyComponent({ title }: Props) {
  // 4. Hooks 调用
  const [state, setState] = useState();
  
  // 5. 事件处理
  const handleClick = () => {};
  
  // 6. 渲染
  return <div>{title}</div>;
}
```

## 📊 项目架构

### 技术架构图

```
┌─────────────────────────────────────────────────────────┐
│                    Next.js App Router                    │
├─────────────────────────────────────────────────────────┤
│  Pages (SSR/SSG)  │  Components  │  Client Components  │
├─────────────────────────────────────────────────────────┤
│         React Query        │        Zustand             │
│      (Server State)        │     (Client State)         │
├─────────────────────────────────────────────────────────┤
│                     Axios Client                         │
│              (Interceptors + Token Auth)                 │
├─────────────────────────────────────────────────────────┤
│                    Backend REST API                      │
│                (Spring Boot + MySQL)                     │
└─────────────────────────────────────────────────────────┘
```

### 数据流

```
User Action → Component → React Query/Zustand → API Client → Backend
                  ↓                                              ↓
              UI Update ← State Update ← Response ← Data Processing
```

## 🔧 环境配置

### 环境变量

创建 `.env.local` 文件：

```env
# API 基础地址
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# OSS 配置（如需文件上传功能）
NEXT_PUBLIC_OSS_REGION=your-region
NEXT_PUBLIC_OSS_BUCKET=your-bucket
```

### API 接口文档

后端 API 文档位于：`http://localhost:8080/v3/api-docs`

获取最新接口文档：
```bash
curl -X GET "http://localhost:8080/v3/api-docs" -H "accept: application/json"
```

## 🎯 项目特性

### 技术特性
- ✅ **TypeScript 严格模式**：完整的类型定义，减少运行时错误
- ✅ **Next.js 14 App Router**：最新的路由系统，支持服务端组件
- ✅ **响应式设计**：移动端优先，多端适配
- ✅ **代码规范**：ESLint + Prettier 自动格式化
- ✅ **类型安全**：API 调用、组件 Props 全面类型化
- ✅ **现代化 UI**：shadcn/ui 组件库，精美动画效果
- ✅ **状态管理**：Zustand 轻量级状态管理
- ✅ **数据缓存**：React Query 智能数据缓存和刷新

### 业务特性
- ✅ **答案隐藏机制**：模拟真实面试场景
- ✅ **Markdown 支持**：富文本编辑和预览
- ✅ **评论系统**：支持嵌套评论和回复
- ✅ **文件上传**：集成阿里云 OSS
- ✅ **权限管理**：完整的 RBAC 权限控制
- ✅ **进度跟踪**：个人学习记录和统计

## 🗺️ 开发路线图

### ✅ 已完成功能（v1.0）
- [x] 用户认证系统（登录、注册）
- [x] 题库浏览和管理
- [x] 题目详情展示
- [x] 答案隐藏机制
- [x] Markdown 编辑器
- [x] 个人中心
- [x] 评论系统
- [x] 管理后台
- [x] 文件上传（OSS）
- [x] 响应式设计

### 🚧 进行中（v1.1）
- [ ] 批量导入题目功能
- [ ] 题目收藏功能
- [ ] 学习进度可视化
- [ ] 移动端体验优化
- [ ] 搜索功能增强

### 📋 计划中（v1.2）
- [ ] 错题本功能
- [ ] 学习统计面板
- [ ] 题目推荐算法
- [ ] 社区互动功能
- [ ] 题目分享功能

### 🚀 未来规划（v2.0）
- [ ] AI 智能推荐
- [ ] 在线刷题模式
- [ ] 模拟面试功能
- [ ] 学习路径规划
- [ ] 成就系统
- [ ] 排行榜功能

## 🤝 贡献指南

### 如何贡献

我们欢迎所有形式的贡献，包括但不限于：

- 🐛 报告 Bug
- 💡 提出新功能建议
- 📝 改进文档
- 🔧 提交代码修复
- ✨ 开发新功能

### 贡献流程

1. **Fork 项目**
   ```bash
   # Fork 本仓库到你的 GitHub 账号
   ```

2. **克隆项目**
   ```bash
   git clone https://github.com/your-username/mianshiyuan-frontend.git
   cd mianshiyuan-frontend
   ```

3. **创建分支**
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/your-bug-fix
   ```

4. **开发和测试**
   ```bash
   npm install
   npm run dev
   # 进行开发和测试
   ```

5. **提交代码**
   ```bash
   git add .
   git commit -m "feat: 添加某某功能"
   # 或
   git commit -m "fix: 修复某某问题"
   ```

6. **推送到 GitHub**
   ```bash
   git push origin feature/your-feature-name
   ```

7. **创建 Pull Request**
   - 在 GitHub 上创建 PR
   - 描述你的改动
   - 等待代码审查

### Commit 规范

使用语义化的 commit 信息：

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具相关

示例：
```bash
feat: 添加题目收藏功能
fix: 修复登录页面样式问题
docs: 更新 API 文档
```

## 📝 最佳实践

### Next.js 开发建议

1. **使用服务端组件（默认）**
   - 只在需要交互时使用 `"use client"`
   - 减少客户端 JavaScript 体积

2. **合理使用 React Query**
   - 使用 `queryKey` 管理缓存
   - 设置合适的 `staleTime` 和 `cacheTime`
   - 使用 `useInfiniteQuery` 处理分页

3. **优化性能**
   - 使用 `next/image` 优化图片
   - 使用动态导入 `dynamic` 懒加载组件
   - 避免不必要的客户端状态

4. **shadcn/ui 使用技巧**
   - 复用组件变体
   - 使用 `cn()` 工具函数合并类名
   - 自定义主题色

### TypeScript 最佳实践

```typescript
// ✅ 好的做法
interface User {
  id: number;
  name: string;
  email?: string; // 可选属性使用 ?
}

// ✅ 使用类型推导
const [user, setUser] = useState<User | null>(null);

// ✅ API 响应类型化
async function getUser(id: number): Promise<User> {
  const response = await api.get<User>(`/users/${id}`);
  return response.data;
}

// ❌ 避免使用 any
const data: any = await fetchData(); // 不推荐
```

## 🔗 相关链接

- [产品需求文档](./doc/产品需求文档.md)
- [后端 API 文档](http://localhost:8080/v3/api-docs)
- [Next.js 文档](https://nextjs.org/docs)
- [shadcn/ui 文档](https://ui.shadcn.com/)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [React Query 文档](https://tanstack.com/query/latest)

## 📄 许可证

本项目采用 MIT 许可证。详见 [LICENSE](./LICENSE) 文件。

## 👥 团队

- **项目负责人**：面试猿团队
- **技术栈**：Next.js + TypeScript
- **设计理念**：用户体验至上，代码质量优先

## 🙏 致谢

感谢以下开源项目：

- [Next.js](https://nextjs.org/) - React 全栈框架
- [shadcn/ui](https://ui.shadcn.com/) - 精美的 UI 组件库
- [Tailwind CSS](https://tailwindcss.com/) - 实用的 CSS 框架
- [Radix UI](https://www.radix-ui.com/) - 无障碍组件基础
- [React Query](https://tanstack.com/query) - 强大的数据获取库
- [Zustand](https://zustand-demo.pmnd.rs/) - 轻量级状态管理

---

<div align="center">
  <p>⭐ 如果这个项目对你有帮助，请给我们一个 Star！</p>
  <p>Made with ❤️ by 面试猿团队</p>
</div>
